{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/pricing_engine/build/pricing_engine",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}